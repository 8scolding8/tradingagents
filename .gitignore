# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Python (TradingAgents clone is trimmed, but keep guards)
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Node
node_modules/

# Environment / secrets (NEVER commit)
.env
.env.*
*.key
*.pem

# Build artifacts
dist/
build/

# Local screenshots cache
*.tmp

# WorkBuddy project metadata (keep locally, never publish)
.workbuddy/

# Local dev tools (one-shot screenshot scripts)
scripts-take-screenshots*.js
